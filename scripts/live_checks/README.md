# Live Checks

These scripts are optional checks against live or credentialed external systems. They are not required for local development, CI, or the default demo path.

Run them only after configuring the relevant provider credentials and target services.
