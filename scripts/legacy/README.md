# Legacy Scripts

These scripts are retained for historical demos or one-off exploration. Prefer current entry points such as `run_demo_suite.py`, `scripts/northstar_smoke.py`, and the tests under `harness/` for routine development.
