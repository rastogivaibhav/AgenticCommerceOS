import { useEffect, useState } from 'react';import { useParams } from 'react-router-dom';import { getV2Agent } from '../api/northstarAPI';import './StudioProof.css';
export default function V2AgentDetail(){const {id}=useParams();const [data,setData]=useState(null);const [error,setError]=useState(null);useEffect(()=>{getV2Agent(id).then(setData).catch(e=>setError(e.message));},[id]);const a=data?.agent||{};const card=data?.card||{};return <div className="studio-proof-page"><h1 className="studio-proof-title">{a.name||id}</h1>{error&&<p>{error}</p>}<div className="studio-grid"><section className="studio-card"><h2>Overview</h2><p>{a.description}</p><div className="pill-row"><span className="status-pill">{a.status}</span><span className="status-pill">{a.risk_level}</span><span className="status-pill">{a.owner_team}</span></div></section><section className="studio-card"><h2>Agent Card</h2><pre>{JSON.stringify(card,null,2)}</pre></section><section className="studio-card"><h2>Route to Production</h2>{(data?.route_to_production||[]).map(r=><div key={r.agent_id}><p>Stage: {r.current_stage}</p><p>Ready: {String(r.ready_for_production)}</p><p>{(r.required_evidence||[]).join(' • ')}</p></div>)}</section></div></div>}
