# Legacy Assets

This directory holds preserved demos or vendor-specific experiments that are useful for reference but are not part of the canonical runtime.

Current product code should live under `apps/`, `acosplatform/`, `integrations/`, `harness/`, `deploy/`, or `docs/`.

The top-level `ops_api/` package is also legacy/transitional, but it remains in place because existing tests still import it.
